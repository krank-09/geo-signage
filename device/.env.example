# Device settings. start.sh / start.bat create a `.env` from your answers; you can also copy this file to `.env` by hand.

# The URL the presenter gives you. The tunnel root, or the root + /api, both work.
SERVER_URL=https://example-name.trycloudflare.com/api
# Leave DEVICE_ID and REGISTRATION_TOKEN out (or empty) to let the dashboard find this display: it announces itself and an admin claims it
# under Devices > Add device, after which it remembers its identity.
DEVICE_ID=DEV-001
REGISTRATION_TOKEN=XXXX-XXXX-XXXX
# CONNECTION_TYPE=wifi        # optional hint shown in the dashboard while unclaimed

# Local page the agent serves (open http://localhost:<port>)
DISPLAY_PORT=8101

# Open the display page automatically. KIOSK=1 tries Chrome/Edge fullscreen (exit with Alt+F4 / Cmd+Q).
OPEN_BROWSER=1
KIOSK=0

# Location source. Laptops have no GPS, so the default is a simulated road trip.
# GPS_MODE: fixed (stays in one place)  |  sim (drives a route)  |  gpsd (real receiver)
# In the demo exactly ONE display moves (sim); the others stay put (fixed).
GPS_MODE=fixed
PLACE=delhi                              # for fixed. Known places: chandigarh, delhi, jaipur, mumbai, ahmedabad
ROUTE_STEPS=8                            # ticks between places (one tick ~ every 3 s)
ROUTE_DWELL=6                            # ticks spent at each place
ROUTE=chandigarh,delhi,jaipur,mumbai     # for sim: the places it drives through, in order
# Alternative to PLACE for fixed: exact coordinates
# LAT=28.6139
# LNG=77.2090

# Security (all optional). The display keeps a private identity key in .cache/<device>/identity.key - never copy it off this machine.
# DEMO_CONTROLS=1             # enables the demo panel (press "d") on the display page; leave OFF on a real display
# DISPLAY_BIND=127.0.0.1      # the local display page listens on this machine only; 0.0.0.0 opens it to the network (Docker)
# ENROLLMENT_KEY=             # a client's enrollment key, so unclaimed displays announce themselves to that client's dashboard only
